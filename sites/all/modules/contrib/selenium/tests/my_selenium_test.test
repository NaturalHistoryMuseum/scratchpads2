<?php

// Attempt to a custom class that extends tweaks

class MySeleniumTestTestCase extends DrupalSeleniumWebTestCase{

  public static function getInfo(){
    return array(
      'name' => 'Click Link Test',
      'description' => 'Tests the click link method',
      'group' => 'Selenium',
      'browsers' => array(
        'firefox'
      )
    );
  }

  function testClickLinkMethod(){
    $user = $this->drupalCreateUser(array(
      'administer nodes',
      'bypass node access',
      'administer content types',
      'access content overview'
    ));
    
    legal_save_accept(1, 1, 'en', $user->uid);
    // this may give an error message
    $this->drupalLogin($user);
       
  
  
    $this->verboseScreenshot();
 //   $this->clickLink('Home');
    $this->driver->getElement('css=#main-menu li.first a')->click();
    $this->verboseScreenshot();    
    
  }
}
