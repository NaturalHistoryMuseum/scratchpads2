IMPORTANT.

The folder containing this file (leftandright), and the file taxonomy.module.inc
(if present) should both be owned and writable by the web server user.  This is
to allow for automatic updating of the taxonomy.module.inc file in the event of
an update to the core taxonomy module.