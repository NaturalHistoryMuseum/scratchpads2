version: "2"

options:
  compose-file-path: 'docker-compose.yml'
  compose-dev-file-path: 'docker/docker-compose.sync.yml'

syncs:
  docker-sync:
    src: './'
    sync_excludes: ['.gitignore', '.git/', '.idea/', '.DS_Store', 'docker/']